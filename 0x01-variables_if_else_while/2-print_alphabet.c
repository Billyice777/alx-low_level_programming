#include <stdio.h>
#include <stdlib>

/**
 * main - Entry point
 *
 * Return: Always 0 (success)
 */
int main(void)
{
	int ch = 'a';
	while (ch <= 'z')
	{
		putchar(ch);
		c++;
	}
	putchar("\n");
	return (0)
}
